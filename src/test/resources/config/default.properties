# Application
app.baseUrl=https://localhost:8080
app.tenantName=Ni?anta?? Klinik

# Selenium
selenium.browser=chrome
selenium.headless=true
selenium.timeoutSec=20
selenium.poll=250ms
selenium.windowSize=1920x1080
selenium.pageLoadStrategy=normal

# Grid
selenium.grid.enabled=false
selenium.grid.url=http://localhost:4444/wd/hub

# Cucumber
cucumber.tags=@smoke

# Auth
auth.username=${AUTH_USER}
auth.password=${AUTH_PASS}
